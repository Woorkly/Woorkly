# Dépendances (le dossier le plus lourd, ne jamais l'uploader)
node_modules/

# Variables d'environnement
.env
**/ .env
src/.env
src/.env.docker

# Logs (fichiers générés en cas d'erreur)
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Système (fichiers cachés de Windows/Mac)
.DS_Store
Thumbs.db

# Dossiers de build (si tu en as plus tard)
dist/
build/